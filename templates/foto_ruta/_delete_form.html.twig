<form method="post" action="{{ path('app_foto_ruta_delete', {'id': foto_rutum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ foto_rutum.id) }}">
    <button class="btn">Delete</button>
</form>
